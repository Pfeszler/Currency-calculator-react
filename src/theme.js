const { ThemeConsumer } = require("styled-components");

export const theme = {
    color: {
        mint: "hsl(135, 57%, 63%)",
        graphite: "#333",
        white: "#fff",
        red: "#FF0000",
        blackTransparent: "rgba(240, 255, 255, 0.548)"
    },
    breakpoint: {
        mobileMax: 767
    }
}